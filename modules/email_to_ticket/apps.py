from django.apps import AppConfig


class EmailToTicketConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'modules.email_to_ticket'
    verbose_name = 'Email to Ticket'
